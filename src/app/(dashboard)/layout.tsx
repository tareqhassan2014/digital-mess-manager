// Placeholder for dashboard layout
// This will be implemented later with sidebar navigation for authenticated users

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
